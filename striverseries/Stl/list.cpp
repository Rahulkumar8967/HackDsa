#include<bits/stdc++.h>
using namespace std;

// list
void explainList(){
list<int> ls;
ls.push_back(2);
ls.emplace_back(4); // {2,4}

ls.push_front(5); // {2,4}

// rest functions same as vector
// begin , end, rbegin, rend, clear, insert, size, swap



}
int main(){

explainList();


    return 0;
}